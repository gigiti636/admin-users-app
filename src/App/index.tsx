import ThemeProvider from '@/theme/ThemeProvider';
import { StyledRoot } from './styles';

const App = () => {
  return (
    <ThemeProvider>
      <StyledRoot>4rf</StyledRoot>
    </ThemeProvider>
  );
};

export default App;
